"""Model registry: discovery, persistence, and lookup of provider-served models.

Submodules own behavior; callers import from the relevant submodule directly
(``magos.registry.state``, ``magos.registry.refresher``, etc.).
"""
